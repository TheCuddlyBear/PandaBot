const { Command } = require('discord-akairo');

class ShipCommand extends Command {
    constructor(){
        super('ship', {
            aliases: ['ship'],
            category: fun
        })
    }
}